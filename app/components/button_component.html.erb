<% if href %>
  <%= tag.a content, href: href, class: final_classes, **html_options.except(:class) %>
<% else %>
  <%= tag.button content, class: final_classes, type: "button", **html_options.except(:class) %>
<% end %>
